# 📅 دليل تقويم المهام - Tasks Calendar

## 🎯 نظرة عامة

تم إضافة تقويم تفاعلي لعرض وإدارة المهام في صفحة Dashboard الموظفين. يدعم التقويم:
- ✅ عرض المهام على مدار الشهر
- ✅ تغيير حالة المهمة مباشرة
- ✅ عرض تفاصيل كاملة لكل مهمة
- ✅ ألوان مميزة لكل حالة
- ✅ واجهة عربية كاملة

---

## 🚀 المكتبة المستخدمة

**React Big Calendar** - مكتبة شهيرة ومرنة لإنشاء التقاويم التفاعلية
```bash
npm install react-big-calendar
```

---

## 📁 الملفات المضافة

### 1. Types للمهام
**📄 `src/types/employee-tasks.ts`**
```typescript
- TaskStatus (enum): حالات المهمة
- TaskPriority (enum): أولويات المهمة
- EmployeeTask (interface): بنية المهمة
- TaskEvent (interface): للتقويم
- Labels & Colors للعرض
```

### 2. مكون Calendar
**📄 `src/components/employee/tasks-calendar.tsx`**
- عرض المهام في تقويم شهري
- نافذة منبثقة لتفاصيل المهمة
- إمكانية تحديث الحالة

### 3. بيانات تجريبية
**📄 `src/lib/demo-tasks.ts`**
- 12 مهمة تجريبية متنوعة
- تغطي مختلف الحالات والأولويات
- موزعة على الشهر الحالي

### 4. أنماط CSS مخصصة
**📄 `src/styles/calendar.css`**
- تنسيق احترافي
- دعم Dark Mode
- RTL Support

---

## 🎨 حالات المهام والألوان

| الحالة | اللون | الوصف |
|--------|-------|-------|
| **قيد الانتظار** (TODO) | 🔘 رمادي | لم يبدأ العمل بها بعد |
| **قيد التنفيذ** (IN_PROGRESS) | 🔵 أزرق | جاري العمل عليها |
| **مكتملة** (COMPLETED) | 🟢 أخضر | تم الانتهاء منها |
| **معلقة** (ON_HOLD) | 🟡 أصفر | متوقفة مؤقتاً |
| **ملغية** (CANCELLED) | 🔴 أحمر | تم إلغاؤها |

---

## 🎯 أولويات المهام

| الأولوية | اللون | الاستخدام |
|----------|-------|-----------|
| **منخفضة** (LOW) | رمادي فاتح | مهام غير عاجلة |
| **متوسطة** (MEDIUM) | أزرق فاتح | مهام عادية |
| **عالية** (HIGH) | برتقالي | مهام مهمة |
| **عاجلة** (URGENT) | أحمر | تحتاج اهتمام فوري |

---

## 💡 كيفية الاستخدام

### للمستخدم النهائي

#### 1. عرض التقويم
- افتح صفحة Dashboard: `/employee/dashboard`
- ستجد التقويم في الأسفل
- المهام معروضة بألوان حسب حالتها

#### 2. عرض تفاصيل مهمة
- انقر على أي مهمة في التقويم
- ستظهر نافذة منبثقة تحتوي على:
  - العنوان والوصف
  - الحالة الحالية
  - الأولوية
  - تاريخ البدء والانتهاء
  - من عينها
  - ملاحظات إضافية

#### 3. تحديث حالة المهمة
- من نافذة التفاصيل
- اضغط على أحد أزرار الحالة الجديدة
- سيتم تحديث المهمة فوراً
- ستظهر رسالة تأكيد

#### 4. التنقل في التقويم
- **السابق/التالي**: للتنقل بين الأشهر
- **اليوم**: للعودة للشهر الحالي
- **الطرق**: شهر / أسبوع / يوم / جدول أعمال

---

## 👨‍💻 للمطورين

### كيفية إضافة مهام حقيقية

#### 1. إنشاء API للمهام

```typescript
// src/modules/employee-tasks/api/tasks.ts
export const EmployeeTasksApi = {
  getTasks: async () => {
    return await apiClient.get('/employee/tasks');
  },
  
  updateTaskStatus: async (taskId: number, status: TaskStatus) => {
    return await apiClient.put(`/employee/tasks/${taskId}/status`, { status });
  }
};
```

#### 2. إنشاء Hook

```typescript
// src/modules/employee-tasks/hooks/use-tasks.ts
export function useEmployeeTasks() {
  return useQuery({
    queryKey: ['employee-tasks'],
    queryFn: () => EmployeeTasksApi.getTasks(),
  });
}

export function useUpdateTaskStatus() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: ({ taskId, status }: { taskId: number; status: TaskStatus }) =>
      EmployeeTasksApi.updateTaskStatus(taskId, status),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['employee-tasks'] });
    },
  });
}
```

#### 3. تحديث Dashboard

```typescript
// src/app/employee/dashboard/page.tsx
import { useEmployeeTasks, useUpdateTaskStatus } from '@/modules/employee-tasks/hooks/use-tasks';

export default function EmployeeDashboard() {
  // استبدل البيانات التجريبية بالحقيقية
  const { data: tasksData, isLoading } = useEmployeeTasks();
  const { mutate: updateStatus } = useUpdateTaskStatus();
  
  const tasks = tasksData?.data?.tasks || [];
  
  const handleUpdateTaskStatus = (taskId: number, newStatus: TaskStatus) => {
    updateStatus({ taskId, status: newStatus });
  };
  
  // ... باقي الكود
}
```

---

## 🔧 التخصيص

### تغيير ألوان الحالات

في `src/types/employee-tasks.ts`:
```typescript
export const taskStatusColors: Record<TaskStatus, string> = {
  [TaskStatus.TODO]: 'bg-gray-100 text-gray-700 border-gray-300',
  [TaskStatus.IN_PROGRESS]: 'bg-blue-100 text-blue-700 border-blue-300',
  // ... غير الألوان حسب حاجتك
};
```

### تغيير أنماط التقويم

في `src/styles/calendar.css`:
```css
.rbc-today {
  background-color: #dbeafe !important; /* غير لون اليوم الحالي */
}

.rbc-event:hover {
  /* أضف تأثيرات hover مخصصة */
}
```

### إضافة حقول جديدة للمهمة

في `src/types/employee-tasks.ts`:
```typescript
export interface EmployeeTask {
  id: number;
  title: string;
  // ... الحقول الموجودة
  
  // أضف حقول جديدة
  estimated_hours?: number;
  actual_hours?: number;
  attachments?: string[];
}
```

---

## 🎨 الميزات المتقدمة

### 1. عرض أسبوعي
التقويم يدعم عرض أسبوعي - فقط اضغط على "أسبوع" من شريط الأدوات

### 2. عرض يومي
للتركيز على مهام يوم واحد - اضغط على "يوم"

### 3. جدول الأعمال
عرض قائمة بالمهام القادمة - اضغط على "جدول الأعمال"

### 4. RTL Support
التقويم يدعم الاتجاه من اليمين لليسار تلقائياً

### 5. Dark Mode
يتكيف تلقائياً مع وضع الظلام/النور

---

## 📊 الإحصائيات

البطاقات في أعلى Dashboard تحسب تلقائياً من المهام:
- **إجمالي المهام**: عدد جميع المهام
- **قيد التنفيذ**: المهام قيد العمل
- **في الانتظار**: المهام المنتظرة

---

## 🐛 حل المشاكل الشائعة

### المهام لا تظهر؟
```typescript
// تحقق من صيغة التواريخ
const task = {
  start_date: new Date('2024-11-05'), // يجب أن تكون Date object
  end_date: new Date('2024-11-10'),
};
```

### الألوان لا تظهر؟
```typescript
// تأكد من استيراد CSS
import '@/styles/calendar.css';
```

### التقويم غير responsive؟
```css
/* في calendar.css - تحقق من media queries */
@media (max-width: 768px) {
  /* التخصيصات للشاشات الصغيرة */
}
```

---

## 🔄 التحديثات المستقبلية

### خطط التطوير:
- [ ] إضافة Drag & Drop لتغيير تواريخ المهام
- [ ] تصفية المهام حسب الحالة/الأولوية
- [ ] تصدير المهام إلى PDF
- [ ] إشعارات للمهام القادمة
- [ ] ربط المهام بالعملاء والمشاريع

---

## 📝 ملاحظات مهمة

1. **البيانات التجريبية**: حالياً يستخدم النظام بيانات تجريبية من `demo-tasks.ts`
2. **الحالة المحلية**: التغييرات تُحفظ في State فقط (تختفي عند إعادة التحميل)
3. **للإنتاج**: يجب ربط النظام بـ Backend API
4. **الأداء**: التقويم محسّن للتعامل مع مئات المهام

---

## 🎓 موارد إضافية

- [React Big Calendar Docs](https://jquense.github.io/react-big-calendar/examples/index.html)
- [date-fns Documentation](https://date-fns.org/)
- [React Query Guide](https://tanstack.com/query/latest)

---

**آخر تحديث**: نوفمبر 2024  
**الحالة**: ✅ جاهز للاستخدام  
**المطور**: CRM System Team

---

**استمتع باستخدام التقويم! 📅✨**

